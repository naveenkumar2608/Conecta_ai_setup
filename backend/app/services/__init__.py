# backend/app/services/__init__.py
"""
Service layer — contains all business logic and external service integrations.
"""
# Intentionally minimal to avoid circular imports during startup initialization.